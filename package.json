{
  "name": "grave-reaper",
  "version": "1.0.0",
  "description": "Grave Reaper — Gothic horror action game. Plays in the browser (website mode) and ships as an installable PWA and an Android APK.",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "icons": "python3 tools/make_icons.py",
    "cap:add": "cap add android",
    "cap:sync": "cap sync android",
    "apk": "npm run build:web && cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.0"
  }
}
